<div class="card m-4">
    <a class="card-body" href="{{ path('app_fridge', {'id': this.fridge.id}) }}">
        <h5 class="card-title">{{ this.fridge.id }}</h5>
        <p class="card-text">{{ this.fridge.name }}</p>
    </a>
</div>